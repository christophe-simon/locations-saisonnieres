{% extends 'admin/base.html.twig' %}

{% block title %}Modification de la réservation n° {{ booking.id }}{% endblock %}

{% block body %}
    <div class="container-fluid">
        <h1 class="my-5">Modification de la réservation n° {{ booking.id }}</h1>

        {{ form_start(form) }}
        <div class="row">
            <div class="col">
                {{ form_row(form.startsOn) }}
                {{ form_row(form.endsOn) }}
                {{ form_row(form.comment) }}
            </div>
            <div class="col">
                {{ form_row(form.booker) }}
                {{ form_row(form.ad) }}
                <button type="submit" class="btn btn-success">
                    <i class="fa-solid fa-check"></i>
                    Enregistrer les modifications
                </button>
            </div>
        </div>
        {{ form_end(form) }}

        {# {{ form_widget(form) }} #}

        {# <button type="submit" class="btn btn-primary">
            <i class="fa-solid fa-check"></i>
            Enregistrer les modifications
        </button>

        {{ form_end(form) }} #}

        {# <table class="table table-hover">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Date</th>
                    <th>Auteur</th>
                    <th>Commentaire</th>
                    <th>Note</th>
                    <th>Annonce</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                {% for comment in comments %}
                <tr>
                    <td>{{ comment.id }}</td>
                    <td>{{ comment.createdAt | date('d/m/Y H:i')}}</td>
                    <td><img src="{{ comment.writer.picture }}" alt="{{ comment.writer.fullName }}" class="avatar avatar-mini">{{ comment.writer.fullName }}</td>
                    <td>{{ comment.content }}</td>
                    <td>
                        <span class="badge badge-dark">{{ comment.rating }}</span>
                    </td>
                    <td>{{ comment.ad.title }}</td>
                    <td>
                        <a href="#" class="btn btn-primary"><i class="fa-solid fa-pencil"></i></a>
                        <a href="#" class="btn btn-danger"><i class="fa-solid fa-trash-can"></i></a>
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table> #}
    </div>
{% endblock %}
